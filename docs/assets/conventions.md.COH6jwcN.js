import{_ as t,o as a,c as i,a2 as n}from"./chunks/framework.CdTmk4Xw.js";const m=JSON.parse('{"title":"Code and archive conventions","description":"","frontmatter":{"editLink":true},"headers":[],"relativePath":"conventions.md","filePath":"conventions.md"}'),o={name:"conventions.md"};function s(r,e,d,c,l,p){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="code-and-archive-conventions" tabindex="-1">Code and archive conventions <a class="header-anchor" href="#code-and-archive-conventions" aria-label="Permalink to &quot;Code and archive conventions&quot;">​</a></h1><p>These conventions apply to new work and edits. Preserve historical code and avoid inventing claims about its style, judging status, or complexity.</p><h2 id="paths-and-naming" tabindex="-1">Paths and naming <a class="header-anchor" href="#paths-and-naming" aria-label="Permalink to &quot;Paths and naming&quot;">​</a></h2><p>Follow each platform&#39;s existing conventions in <a href="./structure.html">Project structure</a>. Use the original problem ID rather than only its title, preserve its case, and prefer stable ASCII filenames for new files. Maintain compatibility with existing paths containing Chinese characters, spaces, or mixed case, and quote paths in commands.</p><p>A problem&#39;s default submission file belongs in its platform directory. To retain alternative implementations or unsuccessful attempts, use meaningful suffixes such as <code>_bruteforce</code>, <code>_stress</code>, or <code>_wa</code>, and document their status in comments or companion notes. Avoid ambiguous backup names such as <code>copy</code> or <code>backup</code>.</p><p>For drafts missing original problem information, preserve a traceable location and status without guessing their platform. Use <code>git mv</code> for confirmed moves, and check CPH import rules, derived website links, and data snapshots. Keep moves separate from algorithm changes.</p><h2 id="individual-solutions" tabindex="-1">Individual solutions <a class="header-anchor" href="#individual-solutions" aria-label="Permalink to &quot;Individual solutions&quot;">​</a></h2><p>Each submission should compile independently on its target judge, without local absolute paths or private headers outside the repository. Use standard input and output, and remove debugging output and local redirection. Check integer ranges, array capacities, recursion depth, and time and space complexity against the problem limits.</p><p>Follow existing indentation for local edits. New files may use four spaces or tabs from the contest template, but should not mix them within a code block. Avoid rewriting historical code solely for formatting. Use UTF-8 for additions and LF line endings in documentation and scripts.</p><p>Comments should explain state definitions, loop invariants, boundary handling, and non-obvious transitions. Avoid narrating simple statements line by line. Short contest variable names are acceptable; public template parameters and preconditions must be understandable.</p><h2 id="provenance-and-validation-status" tabindex="-1">Provenance and validation status <a class="header-anchor" href="#provenance-and-validation-status" aria-label="Permalink to &quot;Provenance and validation status&quot;">​</a></h2><p>When adding a solution or documenting an older one, use a minimal header like this. Record AC verdicts, submission links, or complexity only when evidence supports them. Mark unknown items as unknown or omit them.</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Problem: https://atcoder.jp/contests/abc231/tasks/abc231_a</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Status: sample-checked</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Checks: input 1000 -&gt; output 10.000000</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Time: O(1), Space: O(1)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Status</th><th>Minimum evidence</th></tr></thead><tbody><tr><td><code>draft</code></td><td>Unfinished or not yet run</td></tr><tr><td><code>compile-checked</code></td><td>Recorded compiler, standard, and successful compilation</td></tr><tr><td><code>sample-checked</code></td><td>Listed samples executed and compared; this does not establish correctness on all inputs</td></tr><tr><td><code>ac</code></td><td>Accepted verdict from the judge, with a submission link and judging time where available</td></tr></tbody></table><p>Git commit time records a repository change, not an online judge&#39;s evaluation time. Keep those distinct. Historical files without a status header or provenance receipt have unrecorded status.</p><p>Sources imported through the <a href="https://github.com/xw7qwq/codeflare/blob/docs/project-guide/scripts/README.md" target="_blank" rel="noreferrer">archive tools</a> retain their original content. Validation status and provenance live in <a href="https://github.com/xw7qwq/codeflare/blob/docs/project-guide/archive/submissions.json" target="_blank" rel="noreferrer"><code>archive/submissions.json</code></a>; adding header comments would break verified hashes. Receipts must be checked against actual submission pages and generated by the tools. Never enter an unverified AC verdict manually. When changing imported code, distinguish the new edit from the original submission and correct any integrity claims that no longer apply.</p><p>Place longer explanations in a matching <code>.md</code> file beside the source. Include the original problem link, key idea, correctness argument, complexity, and validation record. Link to the original statement for constraints instead of copying it in full.</p><h2 id="templates" tabindex="-1">Templates <a class="header-anchor" href="#templates" aria-label="Permalink to &quot;Templates&quot;">​</a></h2><p>For new or changed templates, document purpose, interface, index ranges, initialization, numeric limits, complexity, and dependencies. Use at least one minimal program to cover normal calls and boundaries. A template fragment is not a standalone submission, and an existing filename does not establish implementation stability.</p><h2 id="commit-scope" tabindex="-1">Commit scope <a class="header-anchor" href="#commit-scope" aria-label="Permalink to &quot;Commit scope&quot;">​</a></h2><p>Follow <a href="https://github.com/xw7qwq/codeflare/blob/docs/project-guide/.gitignore" target="_blank" rel="noreferrer"><code>.gitignore</code></a> for samples, binaries, logs, and local caches. It uses an allowlist with a default ignore rule. Before adding configuration or files in a new language, check <code>git check-ignore -v &lt;path&gt;</code> and allow precise paths as needed. Avoid force-adding the whole repository.</p><p>See <a href="./contributing.html">Contributing</a> for the process and commit format.</p>`,22)])])}const u=t(o,[["render",s]]);export{m as __pageData,u as default};
