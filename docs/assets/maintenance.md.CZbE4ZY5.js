import{_ as i,o as a,c as s,a2 as t}from"./chunks/framework.CdTmk4Xw.js";const k=JSON.parse('{"title":"Maintenance","description":"","frontmatter":{"editLink":true},"headers":[],"relativePath":"maintenance.md","filePath":"maintenance.md"}'),n={name:"maintenance.md"};function o(r,e,d,h,c,l){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="maintenance" tabindex="-1">Maintenance <a class="header-anchor" href="#maintenance" aria-label="Permalink to &quot;Maintenance&quot;">​</a></h1><h2 id="branch-responsibilities" tabindex="-1">Branch responsibilities <a class="header-anchor" href="#branch-responsibilities" aria-label="Permalink to &quot;Branch responsibilities&quot;">​</a></h2><p><code>main</code> stores the algorithm archive, <code>docs/project-guide</code> stores documentation source and its source-code baseline, and <code>gh-pages</code> stores the problem browser and generated documentation under <code>docs/</code>. Publishing documentation does not require merging it into <code>main</code>. Create temporary branches from the relevant target, validate changes, open a PR, and merge after checks pass. Confirm that the target contains the changes before deleting the temporary branch. Retain all three permanent branches. See the <a href="https://github.com/xw7qwq/.github/blob/main/CONTRIBUTING.md" target="_blank" rel="noreferrer">organization guide</a> and <a href="https://docs.github.com/en/get-started/using-github/github-flow" target="_blank" rel="noreferrer">GitHub flow</a>.</p><p>The documentation branch can merge a newer <code>main</code> through a PR to refresh its source baseline. This is optional catalog maintenance, not a deployment prerequisite. After a documentation PR merges, the workflow directly updates <code>gh-pages/docs/</code>. Archive tools do not commit or push Git automatically; names such as <code>sync/contest-submissions</code> identify temporary import branches, not permanent automation branches. Follow <a href="./pages.html">Pages maintenance</a> for the root problem browser.</p><h2 id="preview-and-deploy-documentation" tabindex="-1">Preview and deploy documentation <a class="header-anchor" href="#preview-and-deploy-documentation" aria-label="Permalink to &quot;Preview and deploy documentation&quot;">​</a></h2><p>Live site: <a href="https://codeflare.lucius7.dev/docs/" target="_blank" rel="noreferrer">CodeFlare Docs</a>. VitePress provides API and project documentation with full-text search, navigation, syntax highlighting, and dark mode.</p><p>Run from the repository root with Node.js 22+:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --prefix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> site</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ci</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --prefix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> site</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>The development server runs at <code>http://127.0.0.1:4174/docs/</code>. The maintained Markdown sources are <code>docs/</code> and the root README and CONTRIBUTING files. Run <code>npm --prefix site run prepare:docs</code> to regenerate content while the server is running, then view the updated pages. Stop the server with <code>Ctrl+C</code>.</p><p>Before publishing, run:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/docs.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --prefix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> site</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check:api</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --prefix</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> site</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span></span></code></pre></div><p>Push the checked documents, configuration, and lockfile to a working branch and open a PR targeting <code>docs/project-guide</code>. Merging starts publication; no additional push to <code>main</code> is needed. Keep generated <code>site/.content/</code>, <code>site/.vitepress/dist/</code>, and <code>node_modules</code> directories out of commits. Dependencies use a patched Vite version pinned in the lockfile. Retain <code>package-lock.json</code> and rerun checks and builds when updating dependencies.</p><p>The publishing workflow validates Markdown, OpenAPI, JSON Schema, and actual snapshots before building the site. Model field tables come directly from the schemas. Source links are converted to GitHub links so deployed pages do not point to nonexistent local directories.</p><p>Only pushes to <code>docs/project-guide</code> publish. The publishing job downloads verified artifacts, confirms that they still correspond to the documentation branch&#39;s latest commit, and verifies the <code>xw7qwq/codeflare</code> repository, the root of <code>gh-pages</code> as the Pages source, and the <code>codeflare.lucius7.dev</code> domain. It replaces only the managed <code>gh-pages/docs/</code> directory. Superseded runs safely skip publication. After checking staged paths, it performs a normal push, explicitly requests a Pages build, and checks public <code>docs/build-info.json</code> and page responses. PRs only validate and build. Inspect <a href="https://github.com/xw7qwq/codeflare/actions" target="_blank" rel="noreferrer">Actions</a> if any step fails; a successful push alone does not establish a successful deployment.</p><p>Build output uses the <code>codeflare-docs</code> management marker. The first migration also accepts the previous <code>competitive-programming-docs</code> marker to update an existing documentation directory. This compatibility applies only after repository, branch, and domain validation; unknown directories are still protected from replacement. When migrating the API repository identity, publish new <code>gh-pages</code> snapshots before documentation that depends on them.</p><p>For a rollback, create a working branch from the documentation source branch, explicitly run <code>git revert &lt;commit-sha&gt;</code> for the problematic commit, validate, and merge a PR back into the source branch. Keep generated <code>gh-pages/docs/</code> content under the publishing workflow&#39;s control; the next build replaces manual edits.</p><h2 id="update-documentation-and-the-source-catalog" tabindex="-1">Update documentation and the source catalog <a class="header-anchor" href="#update-documentation-and-the-source-catalog" aria-label="Permalink to &quot;Update documentation and the source catalog&quot;">​</a></h2><p>Run from a complete clone&#39;s root, handling your uncommitted work first:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --short</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --branch</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fetch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> switch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/refresh-catalog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin/docs/project-guide</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> merge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin/main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/docs.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ref</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin/main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/docs.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --stat</span></span></code></pre></div><p>If the working branch name already exists, choose another clear name or inspect and continue the existing work. Do not overwrite it. Review conflicts individually, preserving the documentation entry points and build configuration alongside the source branch&#39;s archive rules. After merging the source branch, also check directory conventions, tool behavior, and website compatibility. Generated statistics cannot replace these checks.</p><p><code>generate</code> writes only <code>docs/catalog.md</code>. It parses NUL-delimited Git paths and preserves filenames containing Chinese characters or spaces. It records the complete source commit SHA and commit time, rather than the machine&#39;s current date. Counts describe source and other tracked files in the selected commit, excluding uncommitted working files.</p><p>Commit explanation updates with the generated catalog:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/catalog.md</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Add any manually edited documentation paths individually.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cached</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cached</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> commit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;docs(catalog): refresh source inventory&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> HEAD</span></span></code></pre></div><p>Open a PR targeting <code>docs/project-guide</code> and complete documentation builds, API checks, and archive checks before merging. No empty commit is needed if neither the catalog nor documentation changed. If the website also depends on the source changes, open a separate snapshot update PR targeting <code>gh-pages</code>.</p><h2 id="local-checks-and-ci" tabindex="-1">Local checks and CI <a class="header-anchor" href="#local-checks-and-ci" aria-label="Permalink to &quot;Local checks and CI&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/docs.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span></span></code></pre></div><p>The checker uses Git and the Python 3.10+ standard library. It verifies required documents, one top-level heading, basic whitespace, code fences, relative repository links, and Markdown heading anchors. It recalculates the catalog from its recorded commit and checks the checkout&#39;s source paths against the baseline. Track new documentation and local link targets with <code>git add &lt;path&gt;</code> first; a file that exists only in a local working directory is not a publishable link target.</p><p>Checks cover the root <code>README.md</code>, <code>CONTRIBUTING.md</code>, and all Markdown under <code>docs/</code>, without requiring historical notes to be rewritten. The script does not access the network, run algorithms, check every external link or judge result, or establish that all commands work on every system. It supports the inline links and ATX headings used here, rather than implementing a general Markdown renderer.</p><p>The <a href="https://github.com/xw7qwq/codeflare/blob/docs/project-guide/.github/workflows/docs.yml" target="_blank" rel="noreferrer">documentation workflow</a> validates and builds every PR targeting <code>docs/project-guide</code> and every push to that branch, without path-based skips. Its stable check name is <code>documentation</code>. Whitespace checks cover maintained documentation, configuration, and tools, preserving imported contest sources. The validation job is read-only; a separate publishing job uses <code>contents: write</code> and <code>pages: write</code> to update documentation output and request a Pages build.</p><p>The <a href="https://github.com/xw7qwq/codeflare/blob/docs/project-guide/.github/workflows/archive.yml" target="_blank" rel="noreferrer">archive workflow</a> runs <code>archive</code> on every PR and push to <code>main</code> and <code>docs/project-guide</code>, using offline tests and a read-only provenance audit. <code>main</code> requires <code>archive</code>; the documentation branch requires both <code>archive</code> and <code>documentation</code>. Documentation publishing writes directly to <code>gh-pages/docs/</code>, so the output branch must allow the publishing bot&#39;s writes. See the <a href="https://docs.github.com/en/actions/get-started/quickstart" target="_blank" rel="noreferrer">GitHub Actions documentation</a> for workflow authoring.</p><p>When a check fails, inspect the reported path and reason:</p><table tabindex="0"><thead><tr><th>Failure</th><th>Fix</th></tr></thead><tbody><tr><td>Missing relative link target</td><td>Check case, encoding, branch, and whether a new file is tracked</td></tr><tr><td>Invalid heading anchor</td><td>Update headings and references together</td></tr><tr><td>Catalog differs from its source commit</td><td>Regenerate using the commands above; avoid editing generated sections manually</td></tr><tr><td>Current source paths differ from the baseline</td><td>Synchronize with <code>main</code>, review changes, then generate from <code>origin/main</code></td></tr><tr><td>Missing catalog source commit</td><td>Use a complete clone: <code>git fetch --unshallow origin</code> for a shallow clone, or <code>git fetch origin</code> otherwise</td></tr><tr><td>Git ignores a configuration file</td><td>Run <code>git check-ignore -v &lt;path&gt;</code> and update the precise allowlist as needed</td></tr></tbody></table><h2 id="documentation-quality" tabindex="-1">Documentation quality <a class="header-anchor" href="#documentation-quality" aria-label="Permalink to &quot;Documentation quality&quot;">​</a></h2><p>Add guidance to the document closest to the task and link it from the <a href="./handbook.html">documentation index</a>. Support factual claims with code, configuration, command output, or official sources. Distinguish dated audits from live status. Short examples should identify the working directory, dependencies, and expected result; distinguish pseudocode from commands that have actually been tested.</p><p>Maintain statistics only in the generated catalog. Use a fixed commit when documenting provenance and make it clear when a link follows changing content. Update conventions and validation records when branch strategy, problem naming, data definitions, or tool behavior changes.</p>`,35)])])}const g=i(n,[["render",o]]);export{k as __pageData,g as default};
