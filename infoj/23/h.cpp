#include <bits/stdc++.h>
#include "sort.h"

using i64 = long long;

int main () {
	int n;
	std::cin >> n;

	for (int i = 0; i < n; i ++ ) {
		int a;
		std::cin >> a;
	}

	for (int i = 0; i < n; i ++ ) {
		int a;
		std::cout << a << " ";
	}
	

	return 0;
}