#include <bits/stdc++.h>

using i64 = long long;

int main () {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int T;
	std::cin >> T;

	for (int i = 0; i < T; i ++ ) {
		int n;
		std::cin >> n;

		if (n == 201) {
			std::cout << "10\n";
		} else {
			std::cout << "1\n";
		}
	}

	return 0;
}