#include <bits/stdc++.h>

using i64 = long long;

using real = long double;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	real D;
	std::cin >> D;

	std::cout << std::fixed << D / 100 << "\n";

	return 0;
}