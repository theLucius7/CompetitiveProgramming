#include <bits/stdc++.h>

using i64 = long long;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int X;
	std::cin >> X;

	std::cout << (X >= 3 && X <= 18 ? "Yes" : "No") << "\n";

	return 0;
}