#include <bits/stdc++.h>

using i64 = long long;

void solve() {
	int A, B;
	std::cin >> A >> B;

	std::cout << std::round(1.0 * A / B) << "\n";
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	solve();

	return 0;
}