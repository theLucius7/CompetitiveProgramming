#include <bits/stdc++.h>

using i64 = long long;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int N;
	std::cin >> N;

	std::vector<i64> A(N);
	for (int i = 0; i < N; i++) {
		std::cin >> A[i];
	}

	

	return 0;
}