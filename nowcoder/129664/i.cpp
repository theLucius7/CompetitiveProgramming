#include <bits/stdc++.h>

using i64 = long long;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    
    int N;
    std::cin >> N;

    std::vector<std::tuple<int, int, int>> a(n);
    for (int i = 0; i < n; i++) {
    	int id, t, di
    	std::cin >> a
    }
    
    return 0;
}
