#include <bits/stdc++.h>

using i64 = long long;

using real = long double;

constexpr int inf = 1E8;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	std::string s;
	int n;
	std::cin >> s >> n;

	std::vector<std::string> w(n);
	for (int i = 0; i < n; i++) {
		std::cin >> w[i];
	}

	


	return 0;
}