#include <bits/stdc++.h>

// using namespace std;
using i64 = long long;
using i128 = __int128;
// using real = long double;

// constexpr real eps = 1E-7L;

void solve() {
    int n, k;
    std::cin >> n >> k;

    if (k == 1) {
        std::cout << -1 << "\n";
        return;
    }

    int x = 1;
    for (int l = 0, r = 0; l < n; l++) {
        while (r < n &&)
    }
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t = 1;
    std::cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}

