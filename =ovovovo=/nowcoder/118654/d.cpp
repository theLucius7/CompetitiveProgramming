#include <bits/stdc++.h>

using i64 = long long;

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
　
	int n, k;
	std::cin >> n >> k;

	std::string s;
	std::cin >> s;

	std::vector<int> cnt(26);
	for (int l = 0; l < s.size(); l++) {
		
	}
	

	return 0;
}